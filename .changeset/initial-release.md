---
'@noctcore/showcase-kit': minor
---

First release: the `showcase` CLI and a typed config API that capture an app's views (url mode in headless Chromium, or cdp mode for Electron and WebView2), frame them into README images, export exact-size 16:9 portfolio images with a gallery JSON, and print a README image table.
